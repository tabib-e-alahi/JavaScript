const FName = 'Tabib';
const MName = 'E';
const LName = 'Alahi';

const FullName = FName.concat("-",MName, "-", LName);

console.log(FullName) //Tabib-E-Alahi
//manually 
console.log(FName + '-' + MName + '-' + LName);

const first = "Welcome";
const second = "Here";

console.log(first.concat(" ", second)); // Welcome Here